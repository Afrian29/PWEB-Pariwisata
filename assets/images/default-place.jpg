This is a placeholder file for the default tourism place image. Replace this with an actual image file (JPG, PNG, or GIF) named 'default-place.jpg' to serve as the default image for tourism places that don't have custom images uploaded.

Recommended image dimensions: 800x600 pixels (4:3 ratio) or 1200x800 pixels (3:2 ratio).